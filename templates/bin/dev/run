#!/bin/bash
if [ -f .env ]; then
  RAILS_ENV=${RAILS_ENV:-development} docker-compose -f docker-compose.yml -f docker-compose-env.yml run --rm web $@
else
  RAILS_ENV=${RAILS_ENV:-development} docker-compose run --rm web $@
fi